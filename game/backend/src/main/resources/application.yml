server:
  port: 8081
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  # ── MySQL RDS 연동 (eGov globals.properties 스타일) ──────────────
  datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://database-1.cfqskq0w2nkq.ap-northeast-2.rds.amazonaws.com:3306/project01?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul
    username: admin
    password: aass9728!!QA
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000

  # 세션 설정
  session:
    timeout: 30m

# ── MyBatis (전자정부 스타일) ─────────────────────────────────────
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.visualnovel.model.entity
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 30
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# ── CORS ────────────────────────────────────────────────────────────
app:
  cors:
    allowed-origins:
      - "http://localhost:5173"
      - "http://localhost:3000"
      - "http://15.165.130.85:8080"

logging:
  level:
    jdbc.sqlonly: DEBUG
    jdbc.resultsettable: INFO
    com.visualnovel: DEBUG
